<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
  <% @product_title = line_item.product.title %>
  <td><%= line_item.quantity %>&times;</td>
  <td> <%= line_item.product.title %></td>
</tr>
<tr>
  <td>Price: </td>
  <td class="item_price"><%= number_to_currency(line_item.total_price, :unit => "=N=") %></td>
</tr>

<tr>
  <td> <%= button_to 'Delete Item', line_item, :method => :delete, :data => { :confirm => ":::Do you really want to delete '#{@product_title}'?" }, :remote => true %> </td>
  <td><%= button_to 'Decrement Item', line_item, :method => :patch, :data => { :confirm => ":::Do you really want to decrement '#{@product_title}'?" }, :remote => true %></td>
</tr>
<tr> <td colspan="2"></td> </tr>
<tr> <td colspan="2"></td> </tr>
<tr> <td colspan="2"></td> </tr>
<tr> <td colspan="2"></td> </tr>
<tr> <td colspan="2"></td> </tr>
<tr> <td colspan="2"></td> </tr>

